# GC-002c Plan

1. Add a finite public capture/verification CLI and strict manifest contract.
2. Add deterministic tests for accepted evidence and deliberate corruption/private contamination.
3. Run the one-shot GitHub-hosted observation and retain the artifact even when venue access fails.
4. Inspect the artifact. If accepted, freeze a minimal sanitized fixture and manifest; if rejected, record the exact access/transport failure without relabeling it as market evidence.
5. Remove the automatic branch trigger and leave manual capture available.
6. Record CI, workflow, fixture checksum, and remaining private-evidence gate in the rail and draft PR.
