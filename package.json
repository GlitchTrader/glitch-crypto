{
  "name": "glitch-crypto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Probabilistic crypto cognition inside deterministic venue-native execution.",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "check": "npm run build && npm test",
    "start": "node --enable-source-maps dist/src/index.js",
    "cli": "node --enable-source-maps dist/src/cli.js"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
